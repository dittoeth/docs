# Ditto Token

> WIP: `DITTO` token will be the protocol governance token and be distributed as a reward for system use. The token is an ERC-20 via OpenZeppelin's ERC20, ERC20Permit, ERC20Votes contracts.
